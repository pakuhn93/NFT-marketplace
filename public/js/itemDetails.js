const viewItem = () => {
    console.log('BOOP');
}

const listenItem = () => {
    // need to get buttons by their id but we don't have access to item.item_name
    
    // document.getElementById('itemDetails').addEventListener('click', viewItem);
}

listenItem();